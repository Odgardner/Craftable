// After deploying backend/ with `sam deploy --guided`, paste the ApiUrl
// output here. Leave it null to run with stats/streaks disabled — the
// game works fully without a backend, this just adds to it.
const STATS_API_BASE = "https://dj36ckg4ga.execute-api.us-east-1.amazonaws.com";
